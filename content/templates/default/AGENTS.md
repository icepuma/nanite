---
filename: AGENTS.md
---
# Agent Guide

This repository is `{{ repo_name() }}`.

{{ai:Write 4 to 6 strict, modern markdown bullet lines describing how an AI agent should work in this repository. Use only verified repo context and infer the active ecosystem from manifests and workspace files. Follow current official best practices for each detected ecosystem: for Rust prefer cargo-native workflows, workspace-aware changes, formatting, clippy, and tests; for Node.js/npm prefer package.json scripts, workspaces when present, engines/files awareness, and avoid ad hoc command drift; for Python prefer pyproject.toml, [build-system]/[project] metadata, virtual environments, and modern tool config over legacy setup.py patterns unless the repo already requires them; for Go prefer go.mod-driven module layout, package-by-directory conventions, gofmt/go test, and simple module structure. Keep the bullets practical, imperative, and repository-specific. Tell the agent to preserve the existing toolchain and conventions, avoid inventing new frameworks or migration churn, prefer the project's canonical commands, and update docs/tests/config together when behavior changes.}}
